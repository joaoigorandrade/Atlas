import Foundation

/// Where the app signs in.
///
/// There used to be a live OpenRouter key here too, because two kinds were
/// generated on the device. Nothing is any more: every generation goes through
/// `/api/generate`, where the shared content cache, the spend ceilings and the
/// model fallback chain live — so the only credential left is a publishable one
/// designed to be in a client.
///
/// **This file is not committed** (`.gitignore`); `Secrets.example.swift.txt`
/// is the template. Copy it, fill it in, build.
// Copy to `Secrets.swift` (same directory) and fill in from the web app's
// `.env.local`, which already holds all four values.
public enum Secrets {
    /// Supabase, for auth. The publishable key is designed for clients and RLS
    /// is the real access control, so this is a URL and a public identifier.
    public static let supabaseURL = URL(string: "https://YOUR-PROJECT.supabase.co")!
    public static let supabasePublishableKey = "sb_publishable_…"

}
