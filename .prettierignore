.next
node_modules
.claude
scratch
package-lock.json
tsconfig.tsbuildinfo
*.dc.html

# Xcode owns these — it rewrites them on every asset change, so prettier
# formatting them only guarantees the next Xcode write shows up as churn.
ios/App/Resources/Assets.xcassets
ios/App/Resources/Localizable.xcstrings
